defmodule EnvList do

  def new() do [] end # Create empty map

  def add(map, key, value) do
    # CASES
    # 1. Empty map
    # 2. Something already in the map, but different key
    # 3. Same key in map as the one that is added
  end

  def lookup(map, key) do
    # CASES
    # 1. Empty map
    # 2. Key not in map
    # 3. Key in map
  end

  def remove(key, map) do
    # CASES
    # 1. Empty map
    # 2. Key not in map
    # 3. Key in map
  end

end
